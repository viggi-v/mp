 AREA MyFirst, CODE, READONLY
 EXPORT Reset_Handler

Reset_Handler

START
	MOV R1, #10 ;FIRST NUMBER
	MOV R2, #4 ;SECOND NUMBER
	;MOV R2,#0
LOOP
	MOVS R2, R2, ROR #1
	ADDMI R8, R8, R1
	MOV R1, R1, LSL #1
STOP B LOOP
 END